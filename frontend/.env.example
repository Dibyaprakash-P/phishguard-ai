# Base URL of the PhishGuard AI FastAPI backend.
# Never put secrets in this file: everything prefixed with VITE_ is embedded
# into the client bundle and is publicly visible.
VITE_API_URL=http://localhost:8000
